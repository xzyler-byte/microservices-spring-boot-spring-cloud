spring:
  application:
    name: limits-service 
  cloud:
    config:
      uri:
      - http://localhost:8888
  profiles:
    active:
    - qa
management:
  endpoints:
    web:
      exposure:
        include:
        - '*'
                   
        
        
